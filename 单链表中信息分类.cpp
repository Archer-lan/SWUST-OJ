#include<iostream>
#include<cstdlib>
typedef struct Node *N;
struct Node{
	char data;
	N Next;
};

void Creat(){
	N p=NULL,s=NULL,t=NULL;
	s=(N)malloc(sizeof(struct Node));
	scanf("%c",s->data);
	p=s;
	t=(N)malloc(sizeof(struct Node));
	while
}

int main(){
	N p=NULL;
	
	return 0;
} 
